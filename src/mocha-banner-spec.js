'use strict'


/* eslint-env mocha */
const mochaBanner = require('.')

describe('mocha-banner', () => {
  it('write this test', () => {
    console.assert(mochaBanner, 'should export something')
  })
})
